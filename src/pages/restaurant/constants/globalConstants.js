export const MIN_ORDER_QUANTITY = 1;
export const EMPTY_ARRAY_LENGTH = 0;

export const EMPTY_ARRAY = Object.freeze([]);
export const EMPTY_OBJECT = Object.freeze({});
export const EMPTY_SET = Object.freeze(new Set());
export const EMPTY_FUNCTION = Object.freeze(()=>void(0));
export const EMPTY_STRING = '';
export const DEFAULT_NUMBER = 0;


export const DEFAULT_PROP = {
    string : '',
    int : 0,
    func : ()=>void(0),
    array : [],
    object : {}
}